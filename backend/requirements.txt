Django==5.1.4
djangorestframework==3.15.2
django-environ==0.11.2
django-cors-headers==4.6.0
psycopg[binary]==3.2.3
gunicorn==23.0.0

# The `agent` package (framework-agnostic core) is installed separately,
# editable, from the repo-root pyproject.toml: `pip install -e .` run from
# the repository root. Not listed here because its relative path would be
# ambiguous depending on the caller's working directory.
